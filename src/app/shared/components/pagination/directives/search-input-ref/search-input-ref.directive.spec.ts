import { SearchInputRefDirective } from './search-input-ref.directive';

describe('SearchInputRefDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchInputRefDirective();
    expect(directive).toBeTruthy();
  });
});
