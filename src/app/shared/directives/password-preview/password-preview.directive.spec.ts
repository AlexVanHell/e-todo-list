import { PasswordPreviewDirective } from './password-preview.directive';

describe('PasswordPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
